# Legacy snapshot — tools/graph_assets

Ce dossier est une copie **référence** de l'outil FineCraft `tools/graph_assets` tel qu'il existait dans le mono-repo.

But:
- garder un point de comparaison (API, formats nodes/edges, VFS/projets, etc.)
- éviter de réinventer des détails déjà résolus

Notes:
- Les dossiers générés/lourds (`node_modules`, `dist`, caches) sont volontairement exclus.
- Cette snapshot n'est pas destinée à être exécutée telle quelle depuis ce repo.
